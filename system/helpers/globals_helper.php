<?php

if (!defined('SITE_URL')) {
    exit('No Direct access to page');
}

/**
 * Get a key - value pair from a super global
 *
 * e.g $_SESSION[''], $_GET[''], $_POST['']
 */
function superglobal_value($type, $key = "")
{
    if (isset($type)) {
    }
}
